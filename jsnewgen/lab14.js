let facts = { numPlanets: 8, yearNeptuneDiscovered: 1846 };
let { numPlanets, yearNeptuneDiscovered } = facts;
console.log(numPlanets); // *
console.log(yearNeptuneDiscovered); // **
// numPlanets จะมีค่าเป็น 8 ซึ่งเป็นค่าที่อยู่ใน property numPlanets ของ object facts.
// yearNeptuneDiscovered จะมีค่าเป็น 1846 ซึ่งเป็นค่าที่อยู่ใน property yearNeptuneDiscovered ของ object facts.
// ดังนั้น, ผลลัพธ์ที่ได้คือ 8 และ 1846 ตามลำดับ.